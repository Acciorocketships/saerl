defaults:
  - experiment: vmas
  - algorithm: iddpg
  - task: vmas/flocking
  - model: vanilla_model
  - model@critic_model: vanilla_model
  - _self_

hydra:
  searchpath:
   # Tells hydra to add the default benchmarl configuration to its path
    - pkg://benchmarl/conf

seed: 0

